<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!-- android:textColor="#00FF00"-->
        <!--<item name="android:background">#ffffff</item>-->

        <!--My Styles -->
        <!--<item name="android:textSize">11pt</item>
        <item name="android:textColor">@color/blue</item>
        <item name="android:textColorHint">@color/light_blue</item>-->
        <item name="colorButtonNormal">@color/lightGrey</item>
        <item name="android:windowBackground">@color/app_bg_color</item>
        <item name="android:colorBackground">@color/app_bg_color</item>
        <!--<item name="android:editTextStyle">@style/myEditTextStyle</item>
        <item name="android:checkboxStyle">@style/myCheckBox</item>
        <item name="android:listViewStyle">@style/myListView</item>-->
    </style>

    <!--<style name="ActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">@color/black</item>
        <item name="android:background">@color/black</item>
    </style>

   <style name = "myCheckBox">parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:textColor">@color/blue</item>
        <item name="colorAccent">@color/blue</item>
        <item name="android:button">@drawable/ucheckedbox</item>
        <item name="android:checkMark">@drawable/checkedbox</item>
    </style>

    <style name="MyButton" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/lessblack</item>
    </style>

    <style name="myEditTextStyle" parent="@android:style/Widget.EditText">
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/blue</item>
        <item name="android:layout_height">45dip</item>
    </style>

    <style name="myListView">
        <item name="android:divider">@color/navyblue</item>
        <item name="android:dividerHeight">2px</item>
    </style>-->

</resources>
